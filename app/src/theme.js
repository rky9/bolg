import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: { main: "#fff", light: "skyblue" },
    secondary: { main: "#ccc" },
    otherColor: { main: "#fff" },
  },
});
