import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: { main: "#000", light: "skyblue" },
    secondary: { main: "#ccc" },
    otherColor: { main: "#fff" },
  },
});
